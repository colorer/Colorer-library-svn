- есть пожелание: отображать ошибки в базе, типа файл не найден и другие
	примерное решение
		FarEditorSet.cpp:
  	parserFactory->getHRCParser()->setErrorHandler(mySuperCoolErrorHandler);
		выводить на экран, сообщение "есть ошибки, смотрите лог в таком то файле"

- Colorer 5 beta4 понимала формат NMake makefile. В beta5 поддержку этого формата поломали. 
		Хотелось бы вернуть её обратно. Пример некорректно раскрашиваемого файла: http://farmanager.com/svn/trunk/unicode_far/makefile_vc
		В той же beta5 появились проблемы с распознаванием некоторых xml файлов. 
		Пример:http://farmanager.com/svn/trunk/misc/msi-installer/installer.wxs

- при отключении плагина не происходит очистка от раскраски редактируемых файлов, которые сейчас не в "фокусе"

- Colorer 5 тормозит просто безбожно. У меня бывает относительно небольшие файлы (в основном C++/C#), 
	но как начинаешь редактировать - я печатаю быстрее, чем он пишет. Этож, блин, текстовый редактор, а 
	не Crysis. Centrino Duo T7500 (2x2.2). На Athlon 3500+ тоже самое. Про 70 мб я уж и не зарекаюсь. 
	В четвертом Colorer все радостно летает. Но в пятом схемы поприличнее (хотя мне не все нравится, но это уже не по теме).

- Немного недоработок в одной из схем
	colorer\hrc\auto\types\auto.jar\types\assa.hrc
	4) Содержимое тегов \p1, \p2, \p4 может иметь отрицательные значения
	6) Теги \p1, \p2, \p4 могут находиться в любой позиции среди других тегов, а не только в последней
	7) Добавить теги \fax и \fay с покраской их другим цветом, поскольку для их работы нужна достаточно свежая версия рендера субтитров
	Примеры неправильно красящихся файлов в папке test