О. Заменяем:
items -> group.


1.Подстановбки:

<any-element name='[NCName]'>
</any-lemenet>

Шаблон элемента. Употребляется везде, где и обычный элемент. 
Может быть заменен с помощью:

(элемент верхнего уровня)
<substitution name='[QName]' action='...'>
	<element ref='...'/>
	<items ref='...'/>
	<any.../>
</substitution>
Имя должно совпадать с именем элемента. Группа может перегружаться внутри redefine согласно обычным правилам.
Внутри допускаются только ссылки на элементы/группы.

По умолчанию для каждого <any-element> задается:
<substitution>
	<any/>
</substitution>

