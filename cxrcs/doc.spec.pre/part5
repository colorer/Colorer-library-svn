if.

Аттрибут ifdef = [QName list] - если определены перечисленные аттрибуты

Аттрибут if:

1. (%QName;)\s+(=~)\s+(\/.*\/|[^\/].*)
2. (%QName;)\s+(=)\s+\S+

1 - совпадение с регекспом.
2 - точное соответствие со строкой.
