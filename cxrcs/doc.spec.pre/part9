Разрешение неоднозначностй
<schema type='unr:tst'>

<import type='urn:foo'>
	<redefine>
		<element name='foo:bar' ...../> <!-- (1) -->
	</redefine>
</import>

<element name='foo:bar' ..../> <!-- (2) -->

<conetent ...>
	<element name='foo:bar' ..../> <!-- 3 -->
</content>

</schema>


Вариант (1):
	Переопределается элемент foo:bar для всех обрашений из unr:tst к схеме urn:foo
	Аналогично hrc:virtual.


Вариант (2):
	Элемент foo:bar из схемы urn:foo в схеме unr:tst не виден, вместо него идет
	обращение к его местному аналогу. Однако при обращении к любому 
	другому элементу из urn:foo по-прежнему foo:bar из схемы urn:foo. 

Варинат (3): 
	Элемент foo:bar из схемы urn:foo не виден внутри данного <conetent>, вместо
	него там используется локальный foo:bar. В любой другой части
	схемы unr:tst используется foo:bar из схемы urn:foo.


Для других <typedefs> это не актуально, т.к они всегда относятся к текущему типу.


Но в случае, если unr:tst и urn:foo совпадают (в результате использования trans-names, 
или при использовании include), для других <typedefs> используются те же правила.
